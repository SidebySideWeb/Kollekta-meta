# Generate with: openssl rand -hex 32
META_ADMIN_PASSWORD=
META_SESSION_SECRET=

# Fixed — do not change. Server asserts 127.0.0.1 and refuses anything else.
HOST=127.0.0.1
PORT=4000

# Local debugging only — never enable in production
# RATE_LIMIT_DISABLED=true
